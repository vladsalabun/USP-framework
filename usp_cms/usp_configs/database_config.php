<?php

    # Реквізити доступу до бази даних:
    $dbname = 'usp_system';             // <- Треба редагувати при встановленні
    $dbUser = 'mysql';                  // <- Треба редагувати при встановленні
    $dbPassword = 'mysql';              // <- Треба редагувати при встановленні

    # Префікс бази даних:
    $prefix = 'usp';                    // <- Треба редагувати при встановленні

    # Таблиці:
    $tables = array(
        'configuration'
    );
    
